# TG Control

Українська веб-панель для Telegram-бота: вибір підключеного каналу, пости, чат наживо,
модерація учасників, налаштування каналу та небезпечна зона видалення.

## Run Locally

**Prerequisites:**  Node.js

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
